{
  "name": "@ubm-klar/api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "echo 'TODO: wire API dev server'",
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'TODO: add API lint rules'",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {},
  "devDependencies": {}
}
