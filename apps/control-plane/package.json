{
  "name": "@ubm-klar/control-plane",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "dev": "node --watch --experimental-strip-types src/main.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@ubm-klar/config": "workspace:*",
    "@ubm-klar/db": "workspace:*",
    "@ubm-klar/shared-types": "workspace:*",
    "fastify": "^5.2.0"
  },
  "devDependencies": {
    "vitest": "^3.2.6"
  }
}
